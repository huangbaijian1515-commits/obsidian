{
  "readOnly": true,
  "minutesListCommand": {
    "executable": "lark-cli.cmd",
    "args": ["minutes", "+search", "--as", "user", "--start", "2026-05-17", "--end", "2026-05-17", "--page-size", "30", "--format", "json"]
  },
  "notes": [
    "The default sync script can generate dates automatically without this config. Use this file only for explicit read-only experiments.",
    "Do not put +upload, edit, update, delete, upload, move, comment, write, patch, put, or other Feishu write commands in this file."
  ]
}
