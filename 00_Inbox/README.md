# Inbox

Temporary landing zone for mobile shares, quick captures, rough ideas, links, and unprocessed snippets.

Keep items here short-lived. During weekly compile, either:

- convert into a source note in `10_Sources/`
- promote into a manual query note in `20_Query/`
- delete or archive if not worth keeping
